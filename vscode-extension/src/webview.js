'use strict';
function html(nonce) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}';"><style nonce="${nonce}">
body{font:13px var(--vscode-font-family);color:var(--vscode-foreground);background:var(--vscode-editor-background);margin:0;padding:24px}h1{font-size:24px;margin:8px 0}h2{font-size:17px}h3{font-size:14px}p{line-height:1.5}.eyebrow{letter-spacing:2px;color:var(--vscode-descriptionForeground);font-size:11px}.muted{color:var(--vscode-descriptionForeground)}.bar{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.cards{display:flex;gap:12px;margin:20px 0;flex-wrap:wrap}.card{padding:14px;border:1px solid var(--vscode-panel-border);border-radius:6px;min-width:120px}.number{display:block;font-size:25px}.layout{display:grid;grid-template-columns:minmax(300px,38%) minmax(400px,1fr);gap:24px}.routes{max-height:620px;overflow:auto;border:1px solid var(--vscode-panel-border);margin-top:12px}.route{display:block;width:100%;text-align:left;background:transparent;color:var(--vscode-foreground);padding:13px;border:0;border-bottom:1px solid var(--vscode-panel-border);cursor:pointer;overflow-wrap:anywhere}.route[aria-pressed="true"]{background:var(--vscode-list-activeSelectionBackground);color:var(--vscode-list-activeSelectionForeground)}.badge{font-size:11px;display:inline-block;border:1px solid var(--vscode-panel-border);border-radius:4px;padding:2px 6px;margin-top:5px}.card.candidate{border-top:3px solid var(--vscode-charts-blue)}.card.ambiguous{border-top:3px solid var(--vscode-charts-orange)}.card.unmapped{border-top:3px solid var(--vscode-charts-red)}button,select,input,textarea{font:inherit}button.action{background:var(--vscode-button-background);color:var(--vscode-button-foreground);border:0;border-radius:3px;padding:8px 12px;cursor:pointer}button.secondary{background:var(--vscode-button-secondaryBackground);color:var(--vscode-button-secondaryForeground)}button:disabled{opacity:.5;cursor:default}select,input,textarea{background:var(--vscode-input-background);color:var(--vscode-input-foreground);border:1px solid var(--vscode-input-border);border-radius:3px;padding:8px;box-sizing:border-box}input,textarea{width:100%}label{display:block;margin:12px 0 6px}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:12px var(--vscode-editor-font-family);max-height:460px;overflow:auto;padding:12px;background:var(--vscode-textCodeBlock-background)}details{margin:14px 0}summary{cursor:pointer}#status{padding:8px 0;min-height:20px}button.link{color:var(--vscode-textLink-foreground);background:transparent;border:0;text-align:left;cursor:pointer;padding:5px 0;display:block;overflow-wrap:anywhere}.finding{border:1px solid var(--vscode-panel-border);padding:14px;margin:12px 0;border-radius:5px}.finding p{white-space:pre-wrap}.warning{border-left:3px solid var(--vscode-charts-orange);padding-left:12px}#bindings{overflow-wrap:anywhere}*:focus-visible{outline:1px solid var(--vscode-focusBorder);outline-offset:2px}@media(max-width:850px){.layout{grid-template-columns:1fr}.routes{max-height:300px}}
</style></head><body><div class="eyebrow">BURPNEXUS / SOURCE REVIEW</div><h1>From captured request to code evidence</h1><p class="muted" id="bindings">Loading local project…</p><div class="bar"><button class="action secondary" id="connect">Change connection</button><button class="action secondary" id="refresh">Refresh map</button><button class="action secondary" id="saveMap">Save map</button></div><div class="cards" id="counts"></div><p id="coverage" class="muted"></p><p class="warning">Matches are static candidates. Runtime routing, middleware and deployment configuration can change reachability. Source and traffic are only sent after you preview and choose a model.</p><div class="layout"><section><h2>1 · Choose captured endpoint</h2><label for="origin">Captured origin</label><select id="origin"></select><label for="filter">Search endpoints</label><input id="filter" placeholder="GET /api/orders"><label for="state">Mapping status</label><select id="state"><option value="all">All statuses</option><option value="candidate">Candidate</option><option value="ambiguous">Ambiguous</option><option value="unmapped">Unmapped</option></select><div class="routes" id="routes"></div><details><summary id="unobservedLabel">Routes without matching traffic</summary><div id="unobserved"></div></details><details><summary>Coverage limits and skipped files</summary><pre id="warnings"></pre></details></section><section><h2 id="selected">2 · Inspect route candidates</h2><div id="matches"></div><p class="muted" id="samples"></p><label for="checklist">Review focus</label><select id="checklist"><option value="comprehensive">Full security review</option><option value="authorization">Authorization / BOLA / tenant isolation</option><option value="injection">Injection / SSRF / file handling</option><option value="session">Authentication / sessions / browser trust</option><option value="logic">Business logic / replay / state transitions</option><option value="bounty">Public bug bounty lessons</option></select><details id="playbookDetails"><summary id="playbookSummary">Built-in security skill — no add-on required</summary><p class="muted">These checks are bundled with the extension. They guide code analysis and manual testing; they are not executed against the application.</p><div id="playbook"></div><button class="action secondary" id="savePlan" disabled>Save manual test plan</button></details><label for="question">Additional context or question</label><textarea id="question" rows="3" maxlength="8000" placeholder="Describe the intended roles, ownership rules and application behavior."></textarea><div class="bar"><button class="action" id="preview" disabled>Prepare evidence preview</button><button class="action secondary" id="addSource" disabled>Add policy / service excerpt</button></div><details id="previewDetails"><summary>3 · Exact model input — inspect before sharing</summary><p>Redaction is best effort. Check source excerpts, personal data and business-sensitive values.</p><pre id="context"></pre><div id="evidence"></div></details><div class="bar"><button class="action" id="ask" disabled>Choose model &amp; send preview</button><button class="action secondary" id="savePack" disabled>Save pack for another assistant</button><button class="action secondary" id="cancel" disabled>Cancel</button></div><p id="status" role="status" aria-live="polite"></p><div id="answer"></div><button class="action secondary" id="saveReport" disabled>Save cited review</button><details id="draftDetails"><summary>Unvalidated model output</summary><pre id="draft"></pre></details></section></div><script nonce="${nonce}">
const vscode=acquireVsCodeApi(), el=id=>document.getElementById(id);let data,selected,previewHash,busy=false,hasReport=false;
const send=(type,extra={})=>vscode.postMessage({type,...extra});
function node(tag,text,parent,cls){const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;if(parent)parent.append(n);return n;}
function link(parent,label,id){const b=node('button',label,parent,'link');b.onclick=()=>send('open',{id});return b;}
function controls(){for(const id of ['connect','refresh','saveMap','origin','filter','state','checklist','question'])el(id).disabled=busy;el('preview').disabled=busy||!selected;el('addSource').disabled=busy||!selected;el('savePlan').disabled=busy||!selected;el('ask').disabled=busy||!previewHash;el('savePack').disabled=busy||!previewHash;el('cancel').disabled=!busy;el('saveReport').disabled=busy||!hasReport;}
function invalidate(){el('status').textContent='Prepare an evidence preview for the current question.';previewHash=undefined;hasReport=false;el('context').textContent='Prepare a new preview for the current selection and question.';el('evidence').replaceChildren();el('answer').replaceChildren();el('draft').textContent='';controls();}
function select(e){if(busy)return;selected=e;invalidate();el('selected').textContent=e.label;el('samples').textContent=e.count+' captures; up to three distinct status/session variants will be sampled.';el('matches').replaceChildren();if(!e.matches.length)node('p','No supported route matched. Check the repository, proxy prefixes, source exclusions or dynamic routing. Traffic-only review remains available.',el('matches'));for(const m of e.matches)link(el('matches'),m.framework+' · '+m.method+' '+m.path+' → '+m.file+':'+m.line,m.id);renderRoutes();}
function renderPlaybook(){
el('playbook').replaceChildren();const p=data?.playbooks?.[el('checklist').value];if(!p)return;
el('playbookSummary').textContent='Built-in skill · '+p.checks.length+' checks · v'+p.version;node('p',p.title,el('playbook'));
if(p.skill){node('p','Skill v'+p.skill.version+' · Sources reviewed '+p.skill.reviewedOn,el('playbook'),'muted');node('p',p.skill.selection,el('playbook'));node('p','Historical reports guide investigation; they are not evidence of a vulnerability in this application.',el('playbook'),'muted');}
for(const c of p.checks){const d=node('details',undefined,el('playbook'));node('summary',c.id+' · '+c.title,d);
if(c.applicability){node('h3','When relevant',d);node('p',c.applicability,d);}
for(const [title,items]of [['Evidence needed',c.evidence],['Source inspection',c.inspect],['Manual test procedure',c.steps]]){node('h3',title,d);const list=node(title==='Manual test procedure'?'ol':'ul',undefined,d);for(const item of items)node('li',item,list);}
node('h3','Expected secure behavior',d);node('p',c.expected,d);node('h3','Regression test',d);node('p',c.regression,d);node('p',c.caution,d,'muted');
if(c.sourceIds?.length){node('h3','Public disclosure background',d);for(const id of c.sourceIds){const r=p.references?.find(r=>r.id===id);if(!r)continue;const b=node('button',r.id+' · '+r.title,d,'link');b.onclick=()=>send('openReference',{id:r.id});node('p',r.author+' · '+r.publisher+(r.reportedOn?' · Reported '+r.reportedOn:''),d,'muted');node('p',r.observation,d);}}
}}
function renderRoutes(){if(!data)return;el('routes').replaceChildren();const q=el('filter').value.toLowerCase();let count=0;for(const e of data.endpoints){if((el('origin').value!=='all'&&e.origin!==el('origin').value)||(el('state').value!=='all'&&e.state!==el('state').value)||!e.label.toLowerCase().includes(q))continue;const b=node('button','',el('routes'),'route');b.setAttribute('aria-pressed',String(selected?.id===e.id));b.disabled=busy;node('span',e.label,b);node('br',undefined,b);node('span',e.state+' · '+e.count+' captures · '+(e.matchCount||e.matches.length)+' matches',b,'badge');b.onclick=()=>select(e);count++;}if(!count)node('p','No endpoints match this filter.',el('routes'));}
el('filter').oninput=renderRoutes;el('state').onchange=renderRoutes;el('origin').onchange=()=>{selected=undefined;invalidate();el('matches').replaceChildren();el('selected').textContent='2 · Choose an endpoint';renderRoutes();};el('question').oninput=invalidate;el('checklist').onchange=()=>{invalidate();renderPlaybook();};
for(const id of ['connect','refresh','saveMap','cancel','saveReport'])el(id).onclick=()=>send(id);
for(const id of ['preview','addSource'])el(id).onclick=()=>{if(selected)send(id,{endpoint:selected.id,checklist:el('checklist').value,question:el('question').value});};
el('savePlan').onclick=()=>{if(selected)send('savePlan',{endpoint:selected.id,checklist:el('checklist').value});};
el('ask').onclick=()=>send('ask',{hash:previewHash});el('savePack').onclick=()=>send('savePack',{hash:previewHash});
window.addEventListener('message',({data:m})=>{
if(m.type==='state'){data=m;renderPlaybook();el('bindings').textContent='Traffic: '+m.corpus+' · Source: '+m.source;el('coverage').textContent=m.coverage;el('counts').replaceChildren();for(const k of ['endpoints','candidate','ambiguous','unmapped']){const c=node('div','',el('counts'),'card '+k);node('span',String(m.counts[k]),c,'number');node('span',k,c);}el('warnings').textContent=m.warnings.join('\\n')||'No indexing warnings. Unsupported route syntax and excluded files can still leave gaps.';el('origin').replaceChildren();const all=node('option','All captured origins',el('origin'));all.value='all';for(const o of [...new Set(m.endpoints.map(e=>e.origin))]){const option=node('option',o,el('origin'));option.value=o;}el('unobservedLabel').textContent=m.unobserved.length+' route declarations without matching captured traffic';el('unobserved').replaceChildren();for(const r of m.unobserved.slice(0,250))link(el('unobserved'),r.method+' '+r.path+' · '+r.file+':'+r.line,r.id);if(m.unobserved.length>250)node('p','First 250 displayed; save the map for the complete list.',el('unobserved'));renderRoutes();if(m.endpoints.length)select(m.endpoints[0]);}
if(m.type==='preview'){previewHash=m.hash;el('context').textContent=m.text;el('previewDetails').open=true;el('evidence').replaceChildren();for(const e of m.evidence)link(el('evidence'),e.id+' · '+e.kind+' · '+e.file+':'+e.line,e.id);el('status').textContent='Preview ready. This exact input is sent to the selected provider.';controls();}
if(m.type==='busy'){busy=m.value;if(busy){hasReport=false;el('answer').replaceChildren();el('draft').textContent='';el('status').textContent='Selecting model / reviewing…';}else if(el('status').textContent==='Selecting model / reviewing…')el('status').textContent='Ready';controls();renderRoutes();}
if(m.type==='draft')el('draft').textContent=m.text;
if(m.type==='error'||m.type==='status'){el('status').textContent=m.text;if(m.type==='error')el('draftDetails').open=true;}
if(m.type==='report'){hasReport=true;el('draftDetails').open=false;el('answer').replaceChildren();node('h2','Review hypotheses',el('answer'));node('p',m.report.summary,el('answer'));node('p',m.report.disclaimer,el('answer'),'muted');for(const f of m.report.findings){const box=node('article','',el('answer'),'finding');node('span',f.severity+' · '+f.confidence+' confidence · needs validation',box,'badge');node('h3',f.title,box);node('p',f.reasoning,box);for(const id of f.evidenceIds){const e=m.report.evidence.find(e=>e.id===id);link(box,id+' · '+e.file+(e.line?':'+e.line:''),id);}for(const [title,values] of [['Missing evidence',f.missingEvidence],['Manual validation',f.validation],['Remediation',[f.remediation]],['Regression test',[f.regressionTest]]]){node('h3',title,box);const list=node('ul',undefined,box);for(const value of values)node('li',value,list);}}if(!m.report.findings.length)node('p','No supported hypotheses returned. This does not establish that the application is secure.',el('answer'));if(m.report.gaps.length){node('h3','Remaining gaps',el('answer'));const list=node('ul',undefined,el('answer'));for(const gap of m.report.gaps)node('li',gap,list);}el('status').textContent='Review complete. Evidence IDs checked; conclusions still require human validation.';controls();}
});send('ready');
</script></body></html>`;
}
module.exports = { html };
