# BurpNexus — Export Analysis

> This repository was generated by **BurpNexus** from Burp Suite traffic.
> Feed this data to **VS Code Copilot** or any AI assistant for security analysis.

---

## Export Information

| Field | Value |
| ----- | ----- |
| Total Items    | `3` |
| Unique Hosts   | `1` |
| Tools Present  | `proxy` |
| Export Time    | `` |
| JSON files     | `3` |
| Markdown files | `0` |

## Site Map Tree Structure

The output mirrors Burp Suite's Site Map hierarchy:

```
contract-export/
├── example.test/
│   ├── api/
│   │   ├── orders/
│   │   └── users/
│   │       └── 12/
│   └── search/
```

---

## Host Summary

| Host | Items | Tools |
| ---- | ----- | ----- |
| `example.test` | `3` | `proxy` |

---

## AI Analysis Tips

Use the following VS Code Copilot prompts to analyse this export:

```
@workspace Analyse all HTTP requests for SQL injection vulnerabilities.
@workspace Find all endpoints that accept user input and check for XSS vectors.
@workspace Summarise all unique API endpoints discovered in this export.
@workspace Identify authentication mechanisms used across all captured requests.
@workspace List all cookies and security headers present in the responses.
@workspace Find requests with sensitive data like passwords, tokens, or PII.
```

---
*Generated by **BurpNexus** v1.0.0*
